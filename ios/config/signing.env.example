# Copy values into a private file outside version control, then export them in
# the shell before running ios/scripts/build-ipa.sh.
APPLE_DEVELOPMENT_TEAM=
IOS_BUNDLE_ID=com.oa.automation.zhiwuben.ios
IOS_EXPORT_METHOD=development
IOS_OUTPUT_DIR=
IOS_DEFAULT_ACCOUNT_ENDPOINT=
IOS_DEFAULT_STT_ENDPOINT=
