# Private RAG Documents

This directory is reserved for future private knowledge sources.

Before enabling ingestion, the server must provide tenant/user isolation, document-level authorization, encryption at rest, deletion and re-indexing, source citations, audit logs, and limits for file type and size. Do not place production documents here until those controls exist.
