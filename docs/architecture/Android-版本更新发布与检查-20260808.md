# Android 版本更新发布与检查

## 目标

以轻量方式完成 Android APK 的服务端发布、登录后检查和用户可控升级。更新检查不阻塞登录，网络异常静默跳过。

## 客户端行为

- 客户端只读取服务端单一的“当前最新版本”元数据，不请求或逐级展示中间版本。
- 更新元数据检查强制走网络且服务端返回 `Cache-Control: no-store`，避免缓存导致旧版本提示。
- 应用进入前台时检查一次，覆盖冷启动和返回应用的场景；已弹出的旧版本提示会被更高 `version_code` 替换。
- 账号会话从未登录切换为已登录时立即检查一次，覆盖应用内登录而不重建 Activity 的场景。
- 可选更新提供“立即更新、稍后提醒、忽略此版本”；忽略只针对同一 `version_code`，新版本仍会提示。
- 强制更新不能被忽略。
- 点击更新后会再次读取当前 manifest，以避免下载期间错过刚发布的更高版本；下载后校验服务端提供的 SHA-256，再调用系统安装器。

## 服务端发布

- `server/config/app-update.json` 是发布元数据来源，必须有正确的 `version_code` 和 `version_name`。
- Server 与 Android 联动发布优先使用 `deploy-remote.ps1 -WithBackend -AndroidApk <apk>`。仅发布 Android 时，完成同等的包名、版本、签名、非 debuggable 和 SHA-256 门禁后，可将 APK 与 manifest 上传到服务器临时目录，再由服务器既有 `publish-android-update.sh` 原子发布；不得手工替换正式目录 APK 或把 debug APK 作为产品更新。
- 部署端校验 APK 包名、`versionCode`、`versionName`、非 debuggable 标志及固定 release 签名证书，再计算 SHA-256 并调用 `publish-android-update.sh`。
- 发布脚本持有互斥锁，拒绝同版、降级和残留的异常高版本。新 APK 与 manifest 均落盘校验后，才原子切换 manifest；失败时旧 manifest 与旧 APK 集合保持不变。
- APK 以 `ZhiWuBen-Android-<version_code>.apk` 保存，清理按数值 `version_code` 完成，不依赖文件修改时间。
- 发布通道固定且只能保留当前最新版和紧邻的上一版本；`--retain` 只能为 `2`，更早 APK 与旧固定文件名包会自动清理。
- `version_code` 必须严格递增；即使连续发布多个版本，元数据接口也只返回最后一次成功发布的最高版本。
- 版本化下载地址使发布切换前已经开始升级的用户仍可获取上一版，但客户端不会再收到上一版更新提示。
- 部署健康检查会读取公开 metadata、下载并校验最新版 SHA-256，同时确认服务器最多只有两版且上一版仍可下载；任一检查失败则发布任务失败。

## API

| 路径 | 用途 |
|---|---|
| `GET /api/app-update/android` | 返回当前发布版本和版本化下载 URL；未发布时为 204 |
| `GET /api/app-update/android/apk/{version_code}` | 下载当前或唯一保留的上一版 APK |
| `GET /api/app-update/android/apk` | 兼容旧客户端，下载当前 APK |

## 发布前检查

1. 提升 Android `versionCode`，并使 `versionCode`、`versionName` 与 manifest 保持一致。
2. 生成固定签名的 `release` APK；部署脚本会再次验证其证书指纹和非 debug 标志。
3. 联动发布使用 `-WithBackend -AndroidApk <release.apk>`；Android-only 发布只能上传到临时目录并调用服务器原子发布脚本，不直接写正式下载目录。
4. 发布后访问 metadata、最新 APK 与上一版 APK 地址；确认更早版本地址为 404。

## 2026-08-17 Android 1.2.22 发布记录

- Android `1.2.22`（versionCode `10222`）使用固定 release 证书构建；证书 SHA-256 与版本库锁定值一致，APK 为非 debuggable。
- APK SHA-256 为 `6923d729b912107fa38faa76ca777e6954ae64668f96466029888310401e3524`，大小为 `19,347,787` 字节；公网版本化端点下载结果与本地发布物完全一致。
- 服务器通过 `publish-android-update.sh` 原子发布，仅保留 `10222` 与上一版 `10221`，旧版 `10220` 已在健康检查通过后清理。
- 公网 metadata 返回 `1.2.22` 和版本化下载地址；模拟器安装保留版 `1.2.21` 后，在登录态前台恢复时成功显示标准更新弹窗，并展示“立即更新、稍后提醒、忽略此版本”。
- 模拟器最终恢复安装 `1.2.22`。本次为 Android-only 发布，没有部署或替换 Server 业务代码。

## 2026-08-18 Android 1.2.23 发布记录

- Android `1.2.23`（versionCode `10223`）使用固定 release 证书构建；证书 SHA-256 与版本库锁定值一致，APK 为非 debuggable。
- APK SHA-256 为 `a3e9414e09f66c3948eaf230c951d83938485c334c2783272f15ccb0a0d49d18`，大小为 `19,347,787` 字节；公网版本化端点下载结果与本地发布物完全一致。
- 服务器通过 `publish-android-update.sh` 原子发布，仅保留 `10223` 与上一版 `10222`；Backend 重启后为 active。
- 公网 metadata 返回 `1.2.23` 和版本化下载地址；模拟器安装保留版 `1.2.22` 后，在登录态前台恢复时成功显示标准更新弹窗，展示版本号、更新说明及“立即更新、稍后提醒、忽略此版本”。
- 模拟器最终恢复安装正式签名的 `1.2.23`，并验证启动页延长动画和仅保留“记录我的成长”标语。本次为 Android-only 发布，没有部署或替换 Server 业务代码。

## 2026-08-18 APK 目录分享页

- `GET /api/app-update/android/apk/` 现在返回公开的 shadcn 风格 APK 发布页，页面列出当前版本和上一保留版本，并提供版本、文件大小、更新时间、SHA-256、下载和复制直链操作。
- 原有 `/api/app-update/android/apk` 与 `/api/app-update/android/apk/{version_code}` 文件下载接口保持不变；目录页与下载接口共用当前/上一版本保留规则，响应使用 `Cache-Control: no-store`。
- 页面已在桌面和 Pixel 5 移动视口完成截图检查；公网 `10225/10224` 均返回 `206 Partial Content`，确认 Range 下载可用。
- Backend 以不可变 release `1.2.18-20260818082843029-apk-directory` 切换上线，STT 进程和 APK 发布目录未改动。

## 2026-08-19 Android 1.2.27 发布记录

- Android `1.2.27`（versionCode `10227`）使用固定 release 证书构建；证书 SHA-256 与版本库锁定值一致，APK 为非 debuggable。
- 本版将本地实时 STT 恢复策略收敛为 3 次、每次 1 秒，并增加 3 秒绝对恢复窗口；窗口到期后自动切换腾讯云，录音继续保存。录音页新增本地恢复、云端转接和云端接管状态条。
- APK SHA-256 为 `94cdabe4a1a651f13df12677c8ef6fadf259de86d014db5f1081b892ddf1bd99`，大小为 `25,102,931` 字节。
- OTA 通过 `publish-android-update.sh` 原子发布，公网 metadata 返回 `10227`；下载 `10227` 与上一版 `10226` 均通过摘要校验，旧版 `10225` 已清理。
- AVD `My_Phone` 覆盖安装和 `1.2.26 -> 1.2.27` 升级启动通过；直接降级被 Room 正确拒绝，符合固定签名和数据安全策略。
