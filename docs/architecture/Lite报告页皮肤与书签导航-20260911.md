# Lite 报告页皮肤与书签导航（2026-09-11）

## 本次调整

- Lite 报告页使用轻量纸张/涂鸦皮肤，保留 Pro 深色玻璃皮肤；颜色、系统栏和卡片样式统一由 `ReportSkin` 提供。
- 参观考察/研学报告的关键要点、轨迹和照片区域采用更紧凑的卡片排版，减少长页面的视觉噪声。
- 录音页左侧会议类型书签隐藏时只保留彩色书签条，展开后使用对应主题色六角形，不再出现白色尖角矩形。

## 验证

- `:app:testDebugUnitTest --tests com.oa.automation.infrastructure.stt.StreamingSttClientTest --tests com.oa.automation.infrastructure.account.AccountApiServiceTest` 通过。
- `:app:compileDebugKotlin` 通过。
- 服务端账户、STT 运行时测试 112 项通过。
- 公网 Lite OTA 元数据与 `/stt-cloud/health` 均返回 HTTP 200。
- Lite `1.2.77 / 10277` 已原子发布，release id 为 `1.2.77-20260911012100953`；APK SHA-256 为 `67c62fa11742f154755fdc09154230b163429022aa7257166f611e112217aae0`，公网 OTA、APK Range、STT 健康检查和双版本保留均通过。

## 边界

Obsidian 目录仅用于开发过程记录；Lite 版本当前仍关闭本地 STT 与模型切换，云端入口由稳定 IPv4 中继承载。
