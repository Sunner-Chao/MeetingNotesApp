# MeetingNotesApp 研学考察架构调研决策

> 文档用途：开发过程的架构镜像与决策记录。
>
> 明确边界：Obsidian 仅供开发者查阅，不属于 MeetingNotesApp 的用户功能、移动端能力、服务端接口或同步依赖。

## 已确认决策

1. 顶层会议类型只保留：通用会议、项目管理、研学考察。
2. 研学考察从“报告模板”升级为独立的 Journey 旅程模式。
3. 内容采用三层形态：StageNote 分段笔记、JourneyEdition 总游记、PublishedPost 发布快照。
4. 暂停录音、暂存本段、暂停旅程、完成旅程、发布笔记必须是不同状态和动作。
5. 旅程级图片不设人为张数上限，采用分批导入、缩略图、断点上传、失败重试和存储配额。
6. GPS 近期只做 EXIF 和事件采点，不默认持续后台轨迹；公开时支持位置降精度和删除。
7. Android 采用 Room + 本地文件作为可信来源，WorkManager/Outbox 负责持久同步。
8. 服务端近期采用模块化单体；真实规模证明需要时再迁移 PostgreSQL、引入独立搜索或推荐服务。
9. 社区只消费用户确认后的发布快照，不直接暴露原始音频、精确位置和未确认内容。
10. 蓝牙耳机和自研硬件后置，BLE 只作为独立控制面，不侵入 Journey 核心领域。

## 核心实体

`Journey`、`JourneyStage`、`CaptureSession`、`MediaAsset`、`LocationEvidence`、`TranscriptSegment`、`StageDraftVersion`、`JourneyEdition`、`PublishedPost`、`SyncOutbox`。

## 推荐路线

1. P0：回归三类模板和已有补丁。
2. P1：本地 Journey/Stage、多图任务、录音与旅程状态、事件采点。
3. P2：阶段证据提取、阶段稿、人工确认和版本快照。
4. P3：总稿合并、私密云同步和冲突处理。
5. P4：社区发布、搜索、互动、审核、位置和 AI 标识。
6. P5：普通蓝牙验证与自研 BLE 控制设备。

## P1 实施记录（2026-08-05）

### 本批已完成

1. Android Room 数据库升级至 v6：新增 `journeys` 与 `journey_stages`，并提供 5→6 显式迁移、外键级联删除、旅程与会议的一对一约束、阶段序号唯一约束。
2. 新增 `JourneyRepository`：创建旅程和首段、暂存当前段、开始下一段、暂停旅程及继续旅程均以独立状态表达；录音的暂停/继续不改变旅程状态。
3. 录音页仅在用户选择“研学考察”后，通过“更多”菜单显式提供“开始研学旅程”；普通会议不会因模板切换而被自动转换。
4. Siri 录音布局增加一条紧凑的“研学旅程 / 当前段”状态，不增加底部并列操作；“暂存本段、暂停旅程、继续旅程”均收纳在现有溢出菜单。
5. 旅程段暂存的正确顺序为：暂存当前段后不创建空白下一段，用户继续旅程时才创建下一段。

### 本批验证

- `:app:testDebugUnitTest` 全量 Android 单元测试通过（2026-08-05）。
- Journey 实体映射、仓储阶段状态机、录音页换会议时的旅程状态清理均有聚焦单元测试。
- Kotlin/KSP、Room DAO、Koin 注入和 Compose 参数链均完成编译验证。

### 下一批边界

- 2026-08-05 已开始 P2 的证据归属基础批次，详见下节；阶段稿与 AI 生成仍未进入本批。
- 本批未实现“完成旅程”、总游记版本、发布快照、社区、云同步、后台 GPS 轨迹或蓝牙设备控制。
- GPS 仍保持现有的 EXIF/事件采点辅助策略，不开启默认持续定位。

## P2-A 实施记录（2026-08-05）

### 本批已完成

1. Android Room 数据库升级至 v7：`transcripts` 与 `meeting_attachments` 增加可空 `journeyStageId`，并建立阶段查询索引；6→7 显式迁移不改写历史数据。
2. 录音、导入音频、人工文本、转写二选一和批量图片导入在事件开始时捕获当前行程段；普通会议及历史资料保持未归属状态。
3. 录音服务将行程段标识传入 WorkManager 后台转写，即使界面离开或后台任务稍后执行，最终转写仍归属录音开始时的行程段。
4. 数据仓储增加按行程段读取转写和图片的接口，为下一批生成独立 `StageDraftVersion` 提供稳定证据边界。
5. 行程状态正在落库时暂时锁定录音主操作，避免快速点击产生缺失阶段归属的研学录音。

### 下一批边界

- 下一批新增 `StageDraftVersion`，使用阶段范围内的转写、图片与位置证据生成可编辑草稿，并保留每次确认的不可覆盖版本。
- 本批不改变现有整场会议纪要生成逻辑，不自动发布内容，不引入社区、云同步、持续 GPS 或蓝牙依赖。

## P2-B 实施记录（2026-08-05）

### 本批已完成

1. Android Room 数据库升级至 v8：新增 `stage_draft_versions`，按“阶段 + 版本号”唯一索引保存 `StageDraftVersion`；阶段删除时草稿版本随外键级联删除。
2. 已暂存阶段可基于该阶段归属的转写、图片及其 GPS/EXIF 线索生成 Markdown 阶段笔记。生成仍复用既有 `LLMEngine.chat`，不会改写整场会议纪要流程。
3. 阶段稿遵循草稿/确认两态：草稿可编辑，确认操作使用条件更新，已确认版本不能再编辑或二次确认；再次生成只新增下一版本，不覆盖历史版本。
4. Siri 录音页仍将次要操作收纳在“更多”菜单：没有阶段稿时生成，有草稿时编辑，确认后查看或生成新版本；状态条仅显示紧凑的“阶段笔记待编辑/已确认”。
5. 在研学旅程已暂存、但下一段尚未开始的间隙，录音、导入音频、导入图片、保存人工文本和保存转写结果都会被阻止，确保每条研学证据都有明确 `journeyStageId`。

### 本批验证

- `StageDraftVersion` 实体映射覆盖状态、证据计数和确认时间。
- `StageDraftRepository` 覆盖首版编号、版本递增、确认后的不可编辑与不可二次确认。
- 阶段稿 Prompt 覆盖行程段、实际图片清单和“不得虚构图片编号”约束。
- Kotlin/KSP、Room、Koin 与 Compose 参数链完成编译验证。

### 后续边界

- 尚未实现 `JourneyEdition` 总游记合并、社区发布、私密云同步、搜索推荐、后台 GPS 轨迹或 BLE 设备控制。
- 当前只呈现最近一个已暂存阶段的笔记入口；完整阶段历史与总游记编排留给 P3。

## P3 实施记录（2026-08-06）

### 本批已完成

1. Android Room 数据库升级至 v9：新增 `journey_editions`，以旅程和版本号建立唯一约束，并保存总游记引用的阶段稿 ID 快照。
2. 总游记只从每个阶段最新的已确认阶段稿生成；阶段稿仍处于草稿状态时不会进入总游记，避免未确认内容被汇总或发布。
3. 总游记同样采用草稿/确认两态：可编辑、可确认、确认后不可覆盖；再次生成会产生新版本，并记录新的阶段稿来源集合。
4. Siri 录音页新增“生成总游记、编辑总游记、查看总游记、生成总游记新版本”入口，继续复用现有“更多”菜单和简洁状态条。
5. 总游记可在旅程暂停、继续或仍处于进行中的阶段间生成，当前只作为本地编辑与确认结果，不自动完成旅程、不自动发布。

### 本批验证

- Room v9、Koin 参数链和 Compose 回调链完成编译验证。
- 总游记实体映射、仓储版本不可覆盖、Prompt 阶段顺序约束和换会议状态清理均有单元测试。
- Android 全量单元测试通过（2026-08-06）。

### 后续边界

- P4 再考虑发布快照、社区浏览互动、搜索推荐、审核和位置脱敏。
- 云同步、冲突合并、后台 GPS 轨迹、蓝牙耳机及 BLE 控制设备仍不在当前实现内。

## P4-A 实施记录（2026-08-06）

### 本批已完成

1. Android Room 数据库升级至 v10：新增 `published_posts` 本地发布快照表，快照绑定已确认的 `JourneyEdition`，并按旅程保存独立版本号。
2. 发布快照始终使用 `PRIVATE_PREVIEW` 可见性；`READY` 只表示本地发布准备完成，不代表已上传、已公开或已进入社区分发。
3. 创建快照时复制总游记正文并自动移除十进制度表示的精确经纬度；快照不包含原始音频、图片文件或 EXIF 信息，并记录坐标脱敏数量和 AI 辅助标识。
4. 发布前必须分别确认隐私检查和内容/图片权利；两项缺一不可进入 `READY`，就绪后可撤回为 `WITHDRAWN`。
5. Siri 录音页继续通过“更多”菜单提供“创建社区发布预览、发布前检查、查看预览、撤回发布准备”，不增加主页面按钮或社区仪表盘。

### 本批验证

- Room v10、Koin 和 Compose 参数链完成编译验证。
- 发布快照实体映射、双确认门槛、就绪后不可修改、撤回状态、坐标脱敏和换会议状态清理均有单元测试。
- Android 全量单元测试通过（2026-08-06）。

### 后续边界

- P4-B 才进入服务端社区接口、公开发布、个人主页、浏览列表、搜索、互动、审核和举报；届时 Android 与 Server 变更必须分批提交。
- 当前不上传任何发布快照，不提供公共链接，也不引入推荐算法、云同步、持续 GPS 或 BLE。

## P4-B1 实施记录（2026-08-06）

### 本批已完成

1. 服务端新增账户所有者隔离的 `community_posts` 表，使用 `(user_id, client_snapshot_id)` 唯一约束实现客户端快照上传幂等；相同快照重试返回原记录，内容变化返回 `409`，避免网络超时后重复创建社区内容。
2. 新增私有社区同步接口：`POST /api/account/community/drafts`、`GET /api/account/community/drafts/{id}`、`POST /api/account/community/posts/{id}/publish` 和 `POST /api/account/community/posts/{id}/withdraw`。接口复用账户 Bearer 会话，所有查询与状态变更均校验内容所有权。
3. 服务端状态机固定为 `private_draft -> published -> withdrawn`；已发布内容可重复发布请求，已撤回内容为终态；暂存草稿也可以主动撤回，不能再次发布。
4. 服务端只接受结构化快照字段，拒绝未声明的原始资源字段；独立校验隐私检查、内容/图片权利、正文长度、来源版本和精确经纬度表达式，不接收图片二进制、音频、EXIF 或原始 GPS。
5. 发布时记录 `moderation_status=pending`，但本批不实现公共信息流、搜索、推荐、评论、点赞、举报或审核后台；`published` 仅表示服务端已接受发布状态，公开分发仍属于后续阶段。
6. Android Room 数据库升级至 v11：新增 `community_sync_outbox`，以本地 `PublishedPost.id` 为主键和服务端 `client_snapshot_id`，持久保存目标操作、远端内容 ID、尝试次数、错误和同步状态。
7. WorkManager 按单个发布快照建立顺序任务链；网络超时后使用同一幂等键重试，发布或撤回在正在上传时发起会排到当前任务之后，最终以最新 Outbox 操作为准，不会因不确定响应重新创建内容。
8. 本地完成双重检查并进入 `READY` 后自动同步为服务端私有草稿；用户仍需在现有“更多”菜单或发布预览中显式选择“同步并发布”，本地 `PublishedPost` 审核状态与服务端同步状态分开保存。
9. Siri 录音页不增加社区仪表盘或主操作按钮，只复用原有菜单、预览对话框和紧凑旅程状态条显示等待同步、私有草稿、发布、撤回或失败状态。

### 本批验证

- 服务端社区状态机与幂等行为单元测试通过（5 项）；覆盖账户所有者隔离、重复请求、内容冲突、隐私/坐标校验和终态撤回。
- FastAPI 路由测试覆盖认证、`201/200` 幂等响应、发布/撤回链路和未知原始资源字段拒绝。
- 使用真实 `web_backend.app` 生命周期完成账户注册、社区草稿创建、发布和撤回集成验证。
- Android 全量 `:app:testDebugUnitTest` 通过；覆盖 Room/KSP 编译、社区 API 请求路径与 Bearer 头、客户端幂等键、Outbox 映射和目标操作切换。

### 下一批边界

- P4-B2 再增加“我的发布”、公开浏览列表、帖子详情和基础审核；媒体对象存储、举报、搜索、推荐、评论和点赞继续拆成后续小批次。
- 本批不做公共浏览页、个人主页、互动、搜索推荐、审核工作台、媒体对象存储、持续 GPS 或 BLE 耳机控制。

## P4-B2 实施记录（2026-08-06）

### 本批已完成

1. 服务端新增独立 `community_moderation` 审核记录表，不修改 P4-B1 已落库的 `community_posts.moderation_status` 约束；审核结论保存通过/拒绝、拒绝说明、审核人和审核时间，可重复复核并保留当前有效结论。
2. 公开可见性采用双重门槛：内容必须保持 `published` 且当前审核结论为 `approved`；待审核、拒绝或已撤回内容不会出现在公开列表，公开详情也统一返回不可查看。
3. 新增账户所有者接口 `GET /api/account/community/posts`，返回本账号的私有草稿、待审核、已通过、需调整和已撤回记录；查询继续按 `user_id` 隔离，不允许跨账号读取。
4. 新增公开接口 `GET /api/community/posts` 和 `GET /api/community/posts/{id}`；公开响应使用字段白名单，不返回用户 ID、客户端快照 ID、旅程 ID、版本关联或审核内部信息，作者首批统一显示匿名标签“研学同行者”。
5. 所有者列表按 `updated_at + id`、公开列表按 `published_at + id` 使用稳定游标分页，单页限制 1 至 50 条，避免内容增长后使用 offset 造成重复或漏项。
6. 新增管理员审核接口 `POST /api/account/community/moderation/{id}`；非管理员返回 `403`，拒绝时必须提供不超过 500 字的调整说明，只有已发布内容可以进入审核。
7. Android 主工作区底栏保持三项，新增“研学”入口；社区页以“发现 / 我的发布”分段呈现公开信息流和账号发布状态，点击公开条目进入独立详情页。
8. Android UI 延续 Siri “simple and effective”原则：单列信息流、8dp 卡片、紧凑状态标签、单一刷新动作，不增加社区仪表盘、推荐入口或装饰性媒体占位。

### 本批验证

- 服务端社区服务与 FastAPI 路由测试共 8 项通过；覆盖审核前不可见、通过后可见、拒绝不可见、撤回立即下线、所有者隔离、匿名公开字段和管理员权限。
- 实际 `web_backend.app` 已接入 8 条社区路由，公开前缀显式绕过旧运维后台凭据中间件，账户所有者与审核接口仍使用账户 Bearer 会话。
- Android 全量 `:app:testDebugUnitTest` 通过；Kotlin、Compose、Koin 与类型安全导航均完成编译，API 测试覆盖公开列表和“我的发布”的路径及 Bearer 头。

### 后续边界

- P4-B3 优先处理多图媒体对象：资源清单、缩略图、分批/断点上传、失败重试、配额和公开前二次 EXIF/坐标清理；不恢复固定 7 张上限。
- 管理员审核工作台、举报、搜索、推荐、评论和点赞继续独立分批；本批不上传图片二进制、音频、EXIF 或原始 GPS，也不引入持续定位和 BLE。

## P4-B3 实施记录（2026-08-06）

### 本批已完成

1. 服务端新增独立的社区图片元数据表。发布快照可关联任意数量的图片，没有固定 7 张上限；总容量受每账户 512 MB 配额、单张原图 24 MB 和缩略图 2 MB 的明确约束。
2. 图片先建立幂等资源清单，再分别上传原图和缩略图。每个变体使用 `Content-Range`、分片 SHA-256 与服务端已接收偏移量完成断点续传，网络中断后不会重新上传已确认分片。
3. Android 在本地把已确认行程段附件重新编码为 JPEG 原图与缩略图，上传前移除 EXIF；服务端再次清理 JPEG/PNG/WebP 的 EXIF、XMP、IPTC 与 PNG 文本元数据，禁止精确 GPS 随公开图片泄漏。
4. 发布操作要求关联的每张图片均处于完整状态；社区公开接口仍只在“帖子已发布且审核通过”时提供缩略图或原图，私有草稿、待审核、拒绝和撤回状态均不提供公开媒体链接。
5. Android 信息流卡片只展示首张缩略图和图片数量；笔记详情采用横向图片带呈现全部图片，保持单列阅读和紧凑操作，不添加社区仪表盘或复杂图片管理界面。

### 本批验证

- 服务端 `test_community_service` 与 `test_community_routes` 共 11 项通过，覆盖图片配额、幂等清单、分片边界/校验、未完成图片禁止审核通过，以及公开图片访问门槛。
- Android 全量 `:app:testDebugUnitTest` 通过；社区 API 测试覆盖媒体清单的 Bearer/JSON，以及分片 `PUT` 的路径、`Content-Range`、SHA-256 和二进制正文。

### 后续边界

- 后续单独评估图片删除/替换、后台批量上传提示、举报与审核工作台；不把原始音频、原始 EXIF、精确 GPS 或 Obsidian 同步引入用户侧社区功能。
- 搜索、目的地/POI 结构化索引、推荐、评论和点赞仍保持独立阶段，避免在当前可审核的信息流上叠加不受控的分发能力。

## P4-B4 实施记录（2026-08-06）

### 本批已完成

1. 发布预览新增多图选择清单。用户可在 `REVIEW` 状态逐张排除或恢复图片，任何数量均可滚动查看；信息流和同步内容只采用当前纳入的图片。
2. 排除是发布快照内可恢复的选择状态，不删除原始研学附件，也不改动本地已净化的媒体文件。这样误操作可恢复，原始资料仍保留在私有研学记录中。
3. 图片选择在进入 `READY` 后锁定。此时同步队列可能已开始上传，锁定保证用户完成隐私和权利确认后，上传内容不会被并发修改。
4. 发布预览继续保持单个紧凑对话框：显示“已纳入/总数”、文件名、同步状态与图标操作，不增加图片管理页、底栏入口或社区仪表盘。

### 本批验证

- Android 全量 `:app:testDebugUnitTest` 通过。
- `PublishedPostMediaStoreTest` 覆盖审核中排除、恢复和就绪后锁定；Room DAO、Koin 构造参数、Compose 回调链均完成编译验证。

### 后续边界

- 下一批优先补充用户举报与管理员审核工作台，使公开信息流具备完整的人工处置闭环。
- 图片替换、已同步私有草稿的远端删除、批量上传进度和目的地/POI 索引继续拆分，避免改变已确认发布快照的可审计性。

## P4-B5 实施记录（2026-08-06）

### 本批已完成

1. 服务端新增 `community_reports` 举报表，举报类别固定为隐私、版权、安全、广告垃圾和其他；举报说明限制 1000 字，同一用户对同一帖子使用唯一约束幂等，重复点击不会增加举报数量。
2. 举报只接受当前“已发布且审核通过”的公开帖子，未审核、拒绝、撤回内容统一返回不可举报；作者不能举报自己的帖子。举报响应不返回举报人 ID。
3. 管理员新增 `GET /api/account/community/moderation` 审核队列，支持待审核、被举报和全部筛选，沿用稳定游标分页；队列只返回帖子必要正文、审核状态、开放举报数量及匿名类别/说明/时间摘要，不返回举报人、客户端快照、旅程关联或原始媒体字段。
4. 现有审核接口继续处理通过/拒绝；每次管理员处置会将该帖开放举报标记为 `resolved`，保证举报不会重复进入同一待处理队列。审核通过的帖子仍需满足已发布门槛，拒绝后立即从公开信息流下线。
5. Android 笔记详情页增加单个举报图标和紧凑类别/说明对话框；登录状态不足、重复提交和服务端权限错误均在当前上下文反馈，不增加社区仪表盘或复杂社交入口。
6. Android 管理员账户操作区增加“社区审核”入口，工作台以单列队列展示待审/被举报内容，提供通过和带说明拒绝；普通账户只看到权限提示。

### 本批验证

- 服务端社区服务与路由测试共 13 项通过，覆盖公开门槛、自举报、无效类别、说明长度、同一举报人幂等、匿名队列和审核关闭举报。
- Android 全量 `:app:testDebugUnitTest` 通过（2026-08-06），覆盖举报/审核 API 的路径、Bearer、JSON 请求体，以及 Compose、Koin 和类型安全导航链路。

### 后续边界

- 举报处置记录目前只保留帖子级当前审核结论与举报生命周期；申诉、审核历史版本、批量处置和审计导出留待运营规模验证后再做。
- 搜索、目的地/POI 索引、推荐、评论和点赞仍保持独立阶段；不把举报人关系、精确 GPS、原始音频或 Obsidian 引入社区公开功能。

## P4-B6 实施记录（2026-08-06）

### 本批已完成

1. 服务端为社区帖子增加显式结构化索引：目的地、日期、天数、行程段标题、主题标签和 POI 分表保存；当前采用 SQLite 关系索引，不提前引入 Elasticsearch 或 FTS5。
2. 公开列表支持关键词、目的地、标签、POI、天数范围和“含图”筛选，并返回只来源于“已发布且审核通过”内容的有限 facets（目的地、标签、POI）。搜索覆盖标题、正文及上述公开元数据。
3. 公开响应继续使用白名单，不返回用户 ID、旅程/版本关联、客户端快照、审核内部字段、精确坐标、原始媒体内部字段或 EXIF/音频数据；私有草稿和未审核内容不会进入索引结果。
4. Android “发现”页增加单列搜索框、目的地与主题菜单、“仅看图片”和清除筛选；信息流卡片和详情只显示目的地、天数、行程段数量与少量标签摘要，不增加推荐仪表盘或热度榜。
5. 发布预览在 REVIEW 阶段允许用户校正目的地、日期、天数、主题标签和 POI；READY 后元数据与图片选择一并锁定。阶段标题继续来自已确认的阶段快照，避免破坏行程来源审计。
6. 目的地自动提取仅作为粗粒度默认值，不能替代 GPS 反向地理编码；空日期和 0 天数表示未知，不作推测。精确 GPS、EXIF、原始音频、私有草稿和未审核内容均不公开。

### 本批验证

- 服务端 `test_community_service` 与 `test_community_routes` 共 15 项通过，覆盖元数据持久化、公开字段白名单、关键词/目的地/标签/POI/天数/含图筛选及 facets。
- Android `:app:testDebugUnitTest --no-daemon` 通过；覆盖 Room v13 迁移后的实体映射、社区 API、媒体存储、同步仓储，以及 REVIEW 元数据校正后 READY 锁定。

### 后续边界

- 后续再评估搜索排序、评论/点赞、专题集合和推荐分发；本阶段不引入社交推荐算法或持续定位。
- Obsidian 只同步架构和研发记录供开发者查看，不属于 App 用户功能，也不参与社区索引。

## 资料边界

- 51CTO 文章用于借鉴领域拆分、统一 API、消息机制和灰度迁移，不作为携程游记架构的官方证据。
- 携程公开页面说明传统长游记已转向发布笔记；本项目采用“分段笔记 + 总游记 + 发布快照”的双层内容策略。
- 小红书只借鉴封面、标签、信息流和互动，不借鉴其社交内容底层模型。
- Android 官方离线优先和照片选择器资料支持本地可信来源、持久队列和多次追加导入。

## 研发检查点

- [ ] 代码中没有把 Obsidian 作为用户功能或同步接口。
- [ ] 研学阶段稿不能覆盖已确认历史版本。
- [ ] 图片编号不得超过实际附件数量。
- [ ] GPS/EXIF 只能辅助判断，无法确认时必须标注待确认。
- [ ] 发布前完成位置脱敏、版权声明和 AI 内容标识。
- [ ] Word 报告与本 Markdown 决策记录保持主要结论一致。
